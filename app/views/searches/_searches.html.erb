<div class="col-sm-4">
  <form id="frmCart">
    <li>
      <div>
        <div class="ih-item circle colored effect1">
          <a href="#">
            <div class="spinner"></div>
              <div class="info">
              <div class="info-back">
                <h3> <%= searches.name %> </h3>
                <p> $<%= searches.price %></p>
              </div>
            </div>
          </a>
        </div>
      </div>
      <div class="product-name">
        <a href="#"><%= searches.name %></a>
      </div>
      <p> $<%= searches.price %> </p>
      <div class="icon">
        <a href="#">
          <button type="button" class="btn btn-default"><%= t ".previous" %>
          </button>
        </a>
        <a>
          <button id="addBCPT-01" type="button" class="btn btn-default">
            <%= t ".previous" %>
          </button>
        </a>
      </div>
    </li>
  </form>
</div>
